import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AppIndicator = () => {
  return (
    <View>
      <Text>AppIndicator</Text>
    </View>
  );
};

export default AppIndicator;

const styles = StyleSheet.create({});
