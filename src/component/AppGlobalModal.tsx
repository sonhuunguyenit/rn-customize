import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AppGlobalModal = () => {
  return (
    <View>
      <Text>AppGlobalModal</Text>
    </View>
  );
};

export default AppGlobalModal;

const styles = StyleSheet.create({});
