import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AppSkeleton = () => {
  return (
    <View>
      <Text>AppSkeleton</Text>
    </View>
  );
};

export default AppSkeleton;

const styles = StyleSheet.create({});
